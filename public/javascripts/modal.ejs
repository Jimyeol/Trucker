<script>
    // Get the modal
    var modal = document.getElementById("cargoModal");
    // Get the button that opens the modal
    var btn = document.getElementById("cargo");
    // Get the <span> element that closes the modal
    var span = document.getElementsByClassName("close")[0];
    // When the user clicks the button, open the modal 
    btn.onclick = function() {
        modal.style.display = "block";
    }
    // When the user clicks on <span> (x), close the modal
    span.onclick = function() {
        modal.style.display = "none";
    }
    // When the user clicks anywhere outside of the modal, close it
    window.onclick = function(event) {
        if (event.target == modal)   {
        modal.style.display = "none";
        }
    }
    $('#regcargo').click(async ()=> {
        var cargoup = document.getElementById('cargoup').value;
        var cargodown = document.getElementById('cargodown').value;
        var tons = document.getElementById('tons').value;
        var weight = document.getElementById('weight').value;
        try {
            const response = await fetch('http://localhost:3000/regcargo', {
                method: 'post',
                headers: {
                    "Content-Type": "application/json"
                },
                body: JSON.stringify({loading: cargoup, landing: cargodown, tons: tons, weight: weight}),
            })
            if(response.ok) {
                console.log('hi');
            } else {
                console.log('Error')
            }
        } catch (err) {
            window.alert(err);
        }
    })

    // Get the modal
    var delmodal = document.getElementById("delcargoModal");
    // Get the button that opens the modal
    var delbtn = document.getElementById("delcargo");
    // Get the <span> element that closes the modal
    var delspan = document.getElementsByClassName("close")[1];
    // When the user clicks the button, open the modal 
    delbtn.onclick = function() {
        delmodal.style.display = "block";
    }
    // When the user clicks on <span> (x), close the modal
    delspan.onclick = function() {
        delmodal.style.display = "none";
    }
    // When the user clicks anywhere outside of the modal, close it
    window.onclick = function(event) {
        if (event.target == delmodal)   {
        delmodal.style.display = "none";
        }
    }
    $('#delcargo').click(async () => {
        try {
            var response = await fetch('http://localhhost:3000//delcargo', {
                method: 'post',
                headers: {
                    "Content-Type": "application/json"
                },
            })
            if(response.ok) {
                
            } else {

            }
        } catch(err) {

        }
    })

        
</script>